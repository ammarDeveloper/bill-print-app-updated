{
  "name": "bill-printing-backend",
  "version": "0.1.0",
  "description": "Serverless API for the Bill Printing app (AWS SAM + Lambda + DynamoDB)",
  "type": "module",
  "private": true,
  "scripts": {
    "lint": "eslint --ext .js src",
    "format": "prettier --write src",
    "test": "echo \"No tests yet\"",
    "invoke": "node local-runner.js",
    "invoke:customers": "node local-runner.js --resource=/customers --method=GET"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.645.0",
    "@aws-sdk/lib-dynamodb": "^3.645.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.30.0",
    "eslint-plugin-node": "^11.1.0",
    "prettier": "^3.3.3"
  }
}

